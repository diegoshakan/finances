 require 'rails_helper'

# This spec was generated by rspec-rails when you ran the scaffold generator.
# It demonstrates how one might use RSpec to test the controller code that
# was generated by Rails when you ran the scaffold generator.
#
# It assumes that the implementation code is generated by the rails scaffold
# generator. If you are using any extension libraries to generate different
# controller code, this generated spec may or may not pass.
#
# It only uses APIs available in rails and/or rspec-rails. There are a number
# of tools you can use to make these specs even more expressive, but we're
# sticking to rails and rspec-rails APIs to keep things simple and stable.

RSpec.describe "/save_moneys", type: :request do
  
  # SaveMoney. As you add validations to SaveMoney, be sure to
  # adjust the attributes here as well.
  let(:valid_attributes) {
    skip("Add a hash of attributes valid for your model")
  }

  let(:invalid_attributes) {
    skip("Add a hash of attributes invalid for your model")
  }

  describe "GET /index" do
    it "renders a successful response" do
      SaveMoney.create! valid_attributes
      get save_moneys_url
      expect(response).to be_successful
    end
  end

  describe "GET /show" do
    it "renders a successful response" do
      save_money = SaveMoney.create! valid_attributes
      get save_money_url(save_money)
      expect(response).to be_successful
    end
  end

  describe "GET /new" do
    it "renders a successful response" do
      get new_save_money_url
      expect(response).to be_successful
    end
  end

  describe "GET /edit" do
    it "render a successful response" do
      save_money = SaveMoney.create! valid_attributes
      get edit_save_money_url(save_money)
      expect(response).to be_successful
    end
  end

  describe "POST /create" do
    context "with valid parameters" do
      it "creates a new SaveMoney" do
        expect {
          post save_moneys_url, params: { save_money: valid_attributes }
        }.to change(SaveMoney, :count).by(1)
      end

      it "redirects to the created save_money" do
        post save_moneys_url, params: { save_money: valid_attributes }
        expect(response).to redirect_to(save_money_url(SaveMoney.last))
      end
    end

    context "with invalid parameters" do
      it "does not create a new SaveMoney" do
        expect {
          post save_moneys_url, params: { save_money: invalid_attributes }
        }.to change(SaveMoney, :count).by(0)
      end

      it "renders a successful response (i.e. to display the 'new' template)" do
        post save_moneys_url, params: { save_money: invalid_attributes }
        expect(response).to be_successful
      end
    end
  end

  describe "PATCH /update" do
    context "with valid parameters" do
      let(:new_attributes) {
        skip("Add a hash of attributes valid for your model")
      }

      it "updates the requested save_money" do
        save_money = SaveMoney.create! valid_attributes
        patch save_money_url(save_money), params: { save_money: new_attributes }
        save_money.reload
        skip("Add assertions for updated state")
      end

      it "redirects to the save_money" do
        save_money = SaveMoney.create! valid_attributes
        patch save_money_url(save_money), params: { save_money: new_attributes }
        save_money.reload
        expect(response).to redirect_to(save_money_url(save_money))
      end
    end

    context "with invalid parameters" do
      it "renders a successful response (i.e. to display the 'edit' template)" do
        save_money = SaveMoney.create! valid_attributes
        patch save_money_url(save_money), params: { save_money: invalid_attributes }
        expect(response).to be_successful
      end
    end
  end

  describe "DELETE /destroy" do
    it "destroys the requested save_money" do
      save_money = SaveMoney.create! valid_attributes
      expect {
        delete save_money_url(save_money)
      }.to change(SaveMoney, :count).by(-1)
    end

    it "redirects to the save_moneys list" do
      save_money = SaveMoney.create! valid_attributes
      delete save_money_url(save_money)
      expect(response).to redirect_to(save_moneys_url)
    end
  end
end
